/**
 * Tipi auto-generati dal database Supabase.
 *
 * Questo file verrà sovrascritto dal comando:
 * npx supabase gen types typescript --project-id <project-id> > src/types/database.ts
 *
 * Non modificare manualmente.
 */

// Why: placeholder vuoto — i tipi verranno generati automaticamente
// nella sessione dedicata allo schema database.
export type Database = Record<string, never>;
